name: cicd

on:
  workflow_dispatch:
  push:
    paths:
      - src/**
#      - Dockerfile
#      - requirements.txt
#      - .github/workflows/**
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      
      - name: Shorten commit id
        shell: bash
        run: |
          echo "COMMIT_ID=${GITHUB_SHA::6}" >> "$GITHUB_ENV"
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      -
        name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: ramesh4500/python-app:${{ env.COMMIT_ID }}

  cd:
    needs: ci
    runs-on: self-hosted
    steps:
      
      - name: Install argocd cli
        shell: bash
        run: |
          curl -ksSL -o argocd https://jill-commorant-jayden.ngrok-free.dev/download/argocd-linux-amd64
          chmod +x argocd
          sudo mv ./argocd /usr/local/bin/argocd

      - name: Argocd app sync
        shell: bash
        run: |
          argocd login jill-commorant-jayden.ngrok-free.dev \
            --insecure \
            --grpc-web \
            --username admin \
            --password ${{ secrets.ARGOCD_PASSWORD }}
          argocd app sync python-pp
